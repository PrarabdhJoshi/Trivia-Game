<!DOCTYPE html>
<html>
  <head>
    <title>TriviaGame</title>
    <%= csrf_meta_tags %>

    <%= stylesheet_link_tag    'application', media: 'all', 'data-turbolinks-track': 'reload' %>
    <%= javascript_include_tag 'application', 'data-turbolinks-track': 'reload' %>
  </head>

  <body>
  <nav>
  <div class="nav-wrapper">
  <%= link_to root_path do %>
    <%= content_tag :span, "Logo", :class => "brand-logo" %>
  <% end %>
    
    <ul id="nav-mobile" class="right hide-on-med-and-down">
    <% if current_user %>
      <li>Signed in as: <%= current_user.name%> - <%= image_tag(@auth['info']['image'], width: '50', height: '50')%></li>
      <li><%= link_to 'Sign Out', 'sign_out', method: :delete%></li>
  ``<% else %>
      <li><%= link_to "auth/github" do %> <%= image_tag('github.png', width: '40', height: '40')%><%end%></li>
    <%end%>
    </ul>
  </div>
</nav>
<% flash.each do |key, value|%>
  <div ><%= value %></div>
<% end %>

<% if current_user %>
  <div >
    <h1>Welcome, <%= current_user.name%></h1>
    <h4>Logged In with: <strong><em><%= @auth['provider'].capitalize %></em></strong> </h4>
    <%= image_tag ('logo.png')%>
  </div>
<% end %>
    <%= yield %>
  <div class="nav-wrapper footer">
    <p>©</p>
  </div>
  </body>
</html>
